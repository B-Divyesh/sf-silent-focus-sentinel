# Visual thesis: the traversal observatory

Silent Focus Sentinel uses a **luminous glass data landscape**. A scripted element order appears as a horizontal signal path through deep navy space. Populated text glows aqua, duplicate text burns amber, and empty label/value text forms a coral void. The imagery makes a compact accessibility-check sequence scannable in seconds.

## Tokens

- Night `#07111F`: page background. Ink `#F5FAFF`: primary text.
- Deep glass `#0E2238` and raised glass `#15314B`: layered surfaces.
- Signal aqua `#71F2D0`: primary action and healthy focus.
- Quiet coral `#FF8F7B`: empty-text danger. Repeat amber `#FFD166`: duplicate warning.
- Mist `#AAC1D4`: secondary text. Grid `#294860`: dividers and focus rails.
- Day treatment uses ice `#EEF8FA`, navy `#102536`, teal `#087A68`, and coral `#A83F32` in report output. The public site stays explicitly dark because a night control-room field makes quiet gaps readable.

All normal text combinations target at least 4.5:1 contrast. Focus uses a 3 px aqua outline plus offset, never color alone; controls on the aqua demo bar switch to a navy outline so it remains visible against that surface.

## Type and spacing

- Display: self-hosted **Space Grotesk**, 600, for compact technical headings. The Latin WOFF2 comes from Fontsource under the SIL Open Font License; its license ships beside the font and Vite emits it as a content-hashed immutable asset. It uses `font-display: optional`: a cold page retains its system fallback rather than shifting when the display face arrives.
- Body and data: system UI; `ui-monospace` for commands and trace values.
- Scale: 14, 16, 20, 28, 44, and 64 px. Body never drops below 16 px.
- Spacing follows 4/8 px increments. Sections use 80–128 px vertical gaps; controls are at least 44 px tall.

## Shape, layout, and interaction

Rounded, clipped glass panes resemble simulator inspection surfaces. Fine grid lines and a left-side signal rail connect evidence instead of presenting unrelated feature cards. The hero is asymmetrical: copy occupies the upper-left while the generated focus landscape cuts diagonally across the right and beneath it.

The signature motion is a one-time focus pulse moving along the trace. It uses opacity and transform for 6 seconds, then stops. With reduced motion, every stop appears at once and nothing moves. Hover raises a surface by 2 px; keyboard focus is stronger than hover.

## Original assets

- `site/public/focus-landscape.webp`: generated for this product with `/opt/fleet/lib/gen-image.sh`, then converted locally to WebP. Prompt: “Abstract luminous glass data landscape for an accessibility developer tool: a deep navy observatory field with one precise horizontal trail of translucent focus nodes, most glowing cyan, one coral hollow silent gap, one amber echo pair, layered glass planes, subtle technical grid, cinematic volumetric depth, wide composition, no text, no logos, no devices, no people, no generic gradient blobs.” Generated by the factory image deployment; project use under the repository MIT license. The coral gap now represents empty measured label/value text, not observed VoiceOver speech.
- `site/public/og-card.webp`: composed locally from the generated landscape with product typography and exact page title. This is social metadata, not user-required text.
- Logo and status marks are hand-made in HTML/CSS/SVG and contain no third-party assets. `site/public/demo-recording.svg` is generated by `site/scripts/capture-demo-recording.mjs` from the release binary's real `silent-focus-sentinel demo` output; it is a self-hosted SVG recording, not a simulated transcript.
